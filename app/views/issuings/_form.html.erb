<%= form_for(@issuing) do |f| %>
  <% if @issuing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issuing.errors.count, "error") %> prohibited this issuing from being saved:</h2>

      <ul>
      <% @issuing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.number_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
